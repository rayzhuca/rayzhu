"use client";

import { useRef, useState } from "react";
import Image from "next/image";

interface MusicElementProp {
    img_src: string;
    data_sound: string;
    className?: string;
}

const MusicElement: React.FC<MusicElementProp> = () => {
    return <div></div>;
};

export default MusicElement;
